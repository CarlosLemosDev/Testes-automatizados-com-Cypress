{
  "name": "cypress_login_tests",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:run:local": "cypress run --config baseUrl=http://localhost/prestashop",
    "cypress:run:mock": "cypress run --env USE_MOCK=true",
    "test": "npm run cypress:run:mock"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^15.5.0"
  }
}
